[project]
name = "xiangdem-proxy"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = ["Flask>=2.3.0", "requests>=2.31.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
